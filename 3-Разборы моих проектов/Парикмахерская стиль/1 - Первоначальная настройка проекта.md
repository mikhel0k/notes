
## Создание файлов конфигурации
Чтобы сделать postgresql бд в докере создал `docker-compose.yaml` файл
```
services:  
  postgres:  
    image: postgres:15  
    container_name: hair_salon_db  
    environment:  
      POSTGRES_DB: ${POSTGRES_DB}  
      POSTGRES_USER: ${POSTGRES_USER}  
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}  
    ports:  
      - "${POSTGRES_PORT}:5432"  
    networks:  
      - app_network  
  
networks:  
  app_network:  
    driver: bridge
```
И запустил контейнер ```docker-compose up -d``` 

А также перед этим `.env` файл чтобы нигде не светить свою конфигурацию
```
POSTGRES_DB=
POSTGRES_USER=  
POSTGRES_PASSWORD=
POSTGRES_PORT=
POSTGRES_HOST=
```

Сделал `requirements.txt` файл для хранения зависимостей
```
alembic==1.17.2  
annotated-doc==0.0.4  
annotated-types==0.7.0  
anyio==4.11.0  
asyncpg==0.31.0  
bcrypt==4.0.1  
click==8.3.1  
databases==0.9.0  
dnspython==2.8.0  
ecdsa==0.19.1  
email-validator==2.3.0  
fastapi==0.122.0  
h11==0.16.0  
idna==3.11  
Mako==1.3.10  
MarkupSafe==3.0.3  
passlib==1.7.4  
phonenumbers==9.0.19  
psycopg2==2.9.11  
pyasn1==0.6.1  
pydantic==2.12.5  
pydantic-settings==2.12.0  
pydantic_core==2.41.5  
python-dotenv==1.2.1  
python-jose==3.5.0  
python-multipart==0.0.20  
rsa==4.9.1  
six==1.17.0  
sniffio==1.3.1  
SQLAlchemy==2.0.44  
starlette==0.50.0  
typing-inspection==0.4.2  
typing_extensions==4.15.0  
uvicorn==0.38.0
```

Создал файл `settings.py` для того чтобы сделать объект pydantic-settings для более удобного взаимодействия с настройками, а не через `.env`
```
import os  
  
from dotenv import load_dotenv  
from pydantic_settings import BaseSettings  
  
load_dotenv()  
  
  
class Settings(BaseSettings):  
    POSTGRES_DB: str  
    POSTGRES_USER: str  
    POSTGRES_PASSWORD: str  
    POSTGRES_HOST: str  
    POSTGRES_PORT: int  
  
    @property  
    def DATABASE_URL(self) -> str:  
        return (f"postgresql+asyncpg://{self.POSTGRES_USER}:{self.POSTGRES_PASSWORD}@"  
                f"{self.POSTGRES_HOST}:{self.POSTGRES_PORT}/{self.POSTGRES_DB}")  
  
    @property  
    def SYNC_DATABASE_URL(self) -> str:  
        return (f"postgresql://{self.POSTGRES_USER}:{self.POSTGRES_PASSWORD}@"  
                f"{self.POSTGRES_HOST}:{self.POSTGRES_PORT}/{self.POSTGRES_DB}")  
  
    class Config:  
        env_file = ".env"  
        env_file_encoding = "utf-8"  
        case_sensitive = True  
  
  
settings = Settings()
```
Также в нем сделал propety поля для получения синхронной и асинхронной ссылки на подключение в бд. 

Прописал `.gitignore` файл
```
# Python  
__pycache__/  
*.py[cod]  
*$py.class  
*.so  
.Python  
build/  
develop-eggs/  
dist/  
downloads/  
eggs/  
.eggs/  
lib/  
lib64/  
parts/  
sdist/  
var/  
wheels/  
*.egg-info/  
.installed.cfg  
*.egg  
MANIFEST  
  
# Virtual environments  
.venv/  
venv/  
env/  
ENV/  
  
# IDE  
.vscode/  
.idea/  
*.swp  
*.swo  
  
# OS  
.DS_Store  
Thumbs.db  
  
*.iws  
*.iml  
*.ipr  
  
*.swp\  
*__pycache__/  
*.env  
  
*.pem
```

И перешел к созданию структуры проекта
## Структура проекта
```
my_project/
├── app/
│   ├── api/
│   │   └── v1/
│   │       ├── endpoints/
│   │       │   ├── auth.py
│   │       │   ├── users.py
│   │       │   └── items.py
│   │       └── __init__.py
│   │
│   ├️ core/
│   │   ├── config.py
│   │   ├── database.py
│   │   ├── security.py
│   │   └── __init__.py
│   │
│   ├️ models/
│   │   └── __init__.py
│   │
│   ├️ schemas/
│   │   └── __init__.py
│   │
│   ├️ repositories/
│   │   └── __init__.py
│   │
│   ├️ services/
│   │   └── __init__.py
│   │
│   ├── dependencies.py
│   ├── main.py
│   └── __init__.py
│
├── migrations/
├── tests/
│
├── requirements.txt
├── .env
├── .gitignore
├── .alembic.ini
├── .docker-compose.yaml
├── .settings.py
```

Ну а также чтобы проинициализировать миграции alembic на моем проекте сделал
```
alembic init migrations
```
